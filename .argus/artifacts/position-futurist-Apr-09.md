# Futurist Position

**Position:** The fitness formula algebraic complexity guarantees we will overfit to the penalties, not the market.
**Evidence:** The fitness formula is `vs_bah * trade_scale * hhi_penalty * dd_penalty * complexity_penalty * regime_mult`. In a data-driven pipeline using Optuna, the optimizer will eventually find the exact mathematical boundary where the HHI penalty is bypassed, rather than a genuinely robust strategy. As the project formalizes its infrastructure, we are locking in a fitness equation that is too fragile for the deep history we are accumulating.