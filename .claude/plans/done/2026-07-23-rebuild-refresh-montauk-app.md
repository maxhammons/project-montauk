# Rebuild & refresh the Montauk app (full: data + viz + shell)

- [x] 1. Refresh data + signal + artifacts + viz — `.venv/bin/python scripts/ops/daily.py` (230 new bars → data through 2026-07-23, viz rebuilt 9.47 MB, signal risk_off, status ok)
- [x] 2. Rebuild the Tauri app shell — `cd app && npm run tauri:build`
  - First attempt failed: stale `target/` cache had a baked-in path to the project's OLD location (`~/Documents/local-sandbox/...`, no longer exists) in Tauri's autogenerated ACL/permissions. Fixed with `cargo clean` (removed 1.5 GiB) → full fresh rebuild succeeded (33.7s compile).
- [x] 3. Install the rebuilt bundle to /Applications — `scripts/ops/app_update.py --install`
  - Fresh bundle only had a linker-signed (executable-only) ad-hoc signature → `--deep --strict` failed ("code has no resources"). Re-signed the whole bundle ad-hoc (`codesign --force --deep --sign -`, identifier com.projectmontauk.montauk) → verify OK → gated install succeeded (repo state preserved).
- [x] 4. Verify — installed `codesign --verify --deep --strict` OK; `app_smoke: ok (0 failures)`; `doctor.py` status=advisory, 0 failures / 0 critical (only advisories: optional LaunchAgents not installed — pre-existing, left alone).
