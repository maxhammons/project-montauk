# Plan Archive

Executed plan files move here after the work is fully enacted and any remaining
follow-up items have been split into active `docs/*NEXT/` notes.

Do not archive mixed-status plans. If a plan is partly complete, keep it in
`docs/*NEXT/` and add a status audit or extract the unfinished work into a
smaller active plan first.

## Archived Plans

- `2026-04-13-marker-roth-overlay-plan.md` — marker-prior/Roth overlay plan;
  marker work is superseded by validation gates and the Roth overlay is
  implemented.
- `Montauk 2.0/` — executed Pine/TradingView excision master and verification
  plans. The remaining `docs/Montauk 2.0/` folder keeps non-plan historical
  context and baseline records.
