pandas>=2.0
numpy>=1.24
requests>=2.28
scipy>=1.11
optuna>=3.0
# Shadow comparator (Layer 1 certification check) — scripts/validation/integrity.py
# requires this engine for the second-opinion backtest. Missing dependency means
# certified_not_overfit can never be True, so it is a hard requirement, not dev-only.
backtesting>=0.3.3
