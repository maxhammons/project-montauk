# Inbox

Entries the classifier was less than 70% confident about. They land here instead of being misfiled.

Triage via `spirit-audit`: approve (moves to correct file with proper ID), or reject (dropped — these never entered the permanent record).

Entries follow the format defined in `README.md`, plus a `Classifier confidence` field.

<!-- Entries below -->
